name: Test Steam API

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test Steam API endpoints
        env:
          STEAM_API_KEY: ${{ secrets.STEAM_API_KEY }}
        run: python test_api.py
